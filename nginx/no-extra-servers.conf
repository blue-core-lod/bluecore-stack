# Placeholder for NGINX_EXTRA_SERVERS (see compose.yaml). Single-domain hosts
# leave that variable unset and get this empty file, so the
# `include /etc/nginx/conf.d/env/extra/*.conf` in nginx/base.conf resolves to
# nothing. Set NGINX_EXTRA_SERVERS=server-alt.conf to serve a second domain.
