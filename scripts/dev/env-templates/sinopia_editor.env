# sinopia_editor local .env — placeholder.
# Intentionally blank for now: the local-dev stack supplies Sinopia's config via
# the compose `environment:` block. Add repo-level overrides here as needed.
